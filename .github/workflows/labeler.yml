name: PR Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Labeler disabled in read-only token mode
        run: |
          echo "PR labeling is currently disabled because GITHUB_TOKEN is read-only in this repository."
          echo "Enable write workflow tokens before re-enabling actions/labeler."
